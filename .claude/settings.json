{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "{ cd \"${CLAUDE_PROJECT_DIR:-.}\" && vp install --frozen-lockfile && node_modules/.bin/skills experimental_install; } >/dev/null 2>&1 || true",
            "async": true,
            "timeout": 300,
            "statusMessage": "Installing agent skills"
          }
        ]
      }
    ]
  }
}
